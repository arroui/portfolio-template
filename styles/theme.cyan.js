const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.cyan,
  // Secondary color
  beta: colors.indigo,
  // Accent color
  accent: colors.indigo,
  // Neutral color
  omega: colors.zinc,
}

module.exports = theme
